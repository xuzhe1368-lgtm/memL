# memL reverse proxy example (HTTPS)
# Replace meml.example.com with your domain.

meml.example.com {
  reverse_proxy 127.0.0.1:8000

  encode gzip

  header {
    X-Content-Type-Options nosniff
    X-Frame-Options DENY
    Referrer-Policy no-referrer
  }
}
